name: myapp
namespace: mynamespace
deployment:
  replicas: 1
  containers:
    - name: myapp
      image: myimage
      tag: latest
      port: 80
ingress:
  ingressClass: nginx
  clusterIssuer: letsencrypt
  hostnames:
    - example.com
  port: 80
